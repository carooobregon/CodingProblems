#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main(){
    vector<int> a = {-1,5,10,20,28,3};
    vector<int> b = {26,134,135,15,17};
    sort(a.begin(), a.end());
    sort(b.begin(), b.end());
    int pointA = 0; 
    int pointB = 0;
    int minDiff = 9999;

    while(pointA < a.size() && pointB < b.size()){
        
    }

}